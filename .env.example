# Optional first-party adapters. Never put provider API keys in VITE_* variables.
# VITE_LLM_ENDPOINT must point to an institution-controlled proxy that validates
# the GYZJ-AI-DRAFT-1 JSON contract. The public demo leaves it empty.
VITE_ASR_ENDPOINT=
VITE_LLM_ENDPOINT=
VITE_TTS_ENDPOINT=
